import { createContext } from 'react';

export const SingleContext = createContext(null);

export const ParentContext = createContext(null);
export const ChildContext = createContext(null);

export const SiblingAContext = createContext(null);
export const SiblingBContext = createContext(null);